`$ docker run -it node:12-stretch` node 12 version and **stretch** version of debian. Debian 9

------------------
`$ docker run -it node:12-stretch bash` will go to the **linux** side of a container.